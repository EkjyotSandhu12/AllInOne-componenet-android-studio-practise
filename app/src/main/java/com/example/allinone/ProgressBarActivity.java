package com.example.allinone;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;
import android.view.View;
import android.widget.Button;
import android.widget.ProgressBar;

public class ProgressBarActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_progress_bar);

/*


 Button increase = findViewById(R.id.button_incr);
        Button decrease = findViewById(R.id.button_decr);
        ProgressBar circleprogressbar = findViewById(R.id.circle_bar);


        increase.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                if(circleprogressbar.getProgress() < 100){
                    circleprogressbar.setProgress(circleprogressbar.getProgress() + 10);
                }
            }
        });
        decrease.setOnClickListener(new View.OnClickListener() {
            @Override
            public void onClick(View view) {
                if(circleprogressbar.getProgress() > 0){
                   circleprogressbar.setProgress(circleprogressbar.getProgress() - 10);

                }
            }
        });
*/

    }
}
